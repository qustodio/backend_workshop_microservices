{{- if .Values.recommendator.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "qbooks.fullname" . }}-recommendator
  labels:
    {{- include "qbooks.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "qbooks.fullname" . }}-recommendator
  minReplicas: {{ .Values.recommendator.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.recommendator.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.recommendator.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.recommendator.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.recommendator.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.recommendator.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
