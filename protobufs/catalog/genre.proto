syntax = "proto3";

package catalog;

service GenreController {
    rpc List(GenreListRequest) returns (stream Genre) {}
}

message Genre {
    int32 id = 1;
    string name = 2;
}

message GenreListRequest {
}